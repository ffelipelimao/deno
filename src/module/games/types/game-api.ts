export type GameAPI = {
    gameID: number,
    steamAppID: number,
    external: string,
    thumb: string
}


export type Game = {
    id: number,
    name: string,
    image: string,
    steamId: number
}